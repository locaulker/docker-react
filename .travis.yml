language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t locaulker/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true locaulker/docker-react npm run test -- --coverage
